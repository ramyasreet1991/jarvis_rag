# ── Jarvis RAG — Local Mac M2 Requirements ───────────────────────────────────
# Python 3.13 + Apple Silicon (MPS). No CUDA, no Airflow.
# Install inside a venv: python3.13 -m venv .venv && source .venv/bin/activate
# Then: pip install -r requirements-local.txt

# Core
python-dotenv>=1.0.1
pydantic>=2.9.2
pydantic-settings>=2.1.0
numpy>=1.26.4
python-dateutil>=2.8.2

# LangChain & RAG
langchain>=0.3.2
langchain-community>=0.3.2
langchain-core>=0.3.9
langchain-text-splitters>=0.3.2
langchain-ollama>=0.2.0

# Vector DB — local (chromadb 0.6+ supports Python 3.13)
chromadb>=0.6.3

# PyTorch — standard wheel works on M2 via MPS backend
# (do NOT install torch-nightly or CUDA wheels on M2)
torch>=2.5.0
torchvision>=0.20.0

# Embeddings & cross-encoder reranker
sentence-transformers>=3.0.1
transformers>=4.44.2

# Retrieval
rank-bm25>=0.2.2

# Content Extraction
feedparser>=6.0.11
beautifulsoup4>=4.12.3
lxml>=5.0.0
trafilatura>=1.12.2
requests>=2.31.0
youtube-transcript-api>=0.6.2
yt-dlp>=2024.1.0
faster-whisper>=1.0.0
podcastparser>=0.6.10

# API Server
fastapi>=0.115.2
uvicorn[standard]>=0.31.1
httpx>=0.27.2

# Optional — Cloud API fallbacks (comment out if not needed locally)
anthropic>=0.34.2
# voyageai>=0.2.3
# cohere>=5.9.4

# Evaluation
ragas>=0.2.0
datasets>=3.0.1

# Monitoring
langfuse>=2.51.0

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
