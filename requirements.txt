# ── Jarvis RAG — RunPod Requirements ─────────────────────────────────────────
# Python 3.11 + CUDA (RTX 4090). Pinned only where strictly necessary.
# LangChain ecosystem uses >= to avoid internal version conflicts.

# ── Core ──────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.9.2
pydantic-settings>=2.1.0
numpy>=1.26.4
pandas>=2.1.3
python-dateutil>=2.8.2
pytz>=2023.3

# ── LangChain & RAG ───────────────────────────────────────────────────────────
langchain>=0.3.2
langchain-community>=0.3.2
langchain-core>=0.3.15
langchain-text-splitters>=0.3.2
langchain-ollama>=0.2.0

# ── LlamaIndex (Phase 2 — Cloud Stack) ───────────────────────────────────────
# llama-index>=0.9.45
# llama-index-embeddings-openai>=0.1.6
# llama-index-vector-stores-pinecone>=0.1.4
# llama-index-llms-openai>=0.1.5

# ── Vector Databases ──────────────────────────────────────────────────────────
chromadb>=0.6.3
# pinecone-client>=3.0.2            # Phase 2 — uncomment if using Pinecone
# qdrant-client>=1.11.3             # Phase 2 — uncomment if using Qdrant

# ── Embeddings & Models ───────────────────────────────────────────────────────
sentence-transformers>=3.0.1
transformers>=4.44.2
torch>=2.6.0       # v2.6+ required due to CVE-2025-32434 (torch.load vulnerability)

# ── Retrieval ─────────────────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Content Extraction ────────────────────────────────────────────────────────
feedparser>=6.0.11
beautifulsoup4>=4.12.3
lxml>=4.9.3
trafilatura>=1.12.2
requests>=2.31.0
youtube-transcript-api>=0.6.2
yt-dlp>=2024.1.0
faster-whisper>=1.0.0

# ── API Server ────────────────────────────────────────────────────────────────
fastapi>=0.115.2
uvicorn[standard]>=0.31.1
httpx>=0.27.2

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.23

# ── Phase 2 — Cloud API Upgrades (uncomment as needed) ───────────────────────
anthropic>=0.34.2
# voyageai>=0.2.3
# cohere>=5.9.4

# ── Evaluation ────────────────────────────────────────────────────────────────
ragas>=0.2.0
datasets>=3.0.1

# ── Monitoring ────────────────────────────────────────────────────────────────
langfuse>=2.51.0
python-logging-loki>=0.3.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.3
pytest-asyncio>=0.21.1
